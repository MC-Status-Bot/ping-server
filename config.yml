environment: development
host: 0.0.0.0
port: 3001
redis: ${REDIS_URL} # Use an environment variable to define the Redis URL
cache:
  enable_locks: true
  java_status_duration: 5m
  bedrock_status_duration: 5m
  icon_duration: 24h
